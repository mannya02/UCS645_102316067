# Inference
Program correctly finds global maximum/minimum and identifies which process produced them using MPI_MAXLOC/MPI_MINLOC. Values vary each execution due to random generation, but the reduction logic remains correct.
