## Inference

In this problem, vector addition was implemented using CUDA, and both theoretical and measured memory bandwidth were analyzed. The measured bandwidth was significantly lower than the theoretical value due to factors such as memory access latency, kernel launch overhead, and non-optimal memory access patterns. This demonstrates the gap between ideal hardware capabilities and real-world performance in GPU computing.
