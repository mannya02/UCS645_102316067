## Inference
GPU outperforms CPU for large vector sizes due to parallel execution, while small workloads favor CPU because of kernel launch and transfer overhead; optimal thread configuration depends on warp efficiency.
