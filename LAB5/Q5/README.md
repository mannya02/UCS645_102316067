### Inference

Parallel execution reduces time because the workload is distributed among processes. Similar to Q4, scalability is limited at higher process counts due to communication overhead and master bottleneck.
