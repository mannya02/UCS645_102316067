### Inference
Since the computation is evenly divided and communication is minimal, execution time decreases significantly as processes increase. At very high process counts, synchronization and reduction overhead limit further speedup.
